name: Publish to PyPI

on:
  release:
    types: [created]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Build package
        run: python -m build

      - name: Publish to PyPI
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI-AgEIcHlwaS5vcmcCJDdjZDEyMDZhLTkzMjAtNGQ1YS1iNTVhLWY4ZTRiNjAwY2VmNwACKlszLCIwNGZlNTI5NC1mNjYzLTQxYmMtYTc2MC02NDdkNDUyOWMzMDEiXQAABiALAGxr8UB0ss2qUkyT4m2LxlOf1r5ppEuMfzQ80gNLPw }}
        run: twine upload dist/*
